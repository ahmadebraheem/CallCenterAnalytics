name: callcenter
version: '1.0.0'
config-version: 2
profile: callcenter
model-paths: [models]
test-paths: [tests]
macro-paths: [macros]
clean-targets: [target, dbt_packages]
models:
  callcenter:
    staging:
      +materialized: view
